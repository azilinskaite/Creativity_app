import React from "react";

export default function Achievements( ) {
    return (
        <section className="p-[2rem]">
            Fetch started disciplines in progress with matching background colors
        </section>
    )
}