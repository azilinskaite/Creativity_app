import React from "react";

export default function Gallery( ) {
    return (
        <section className="p-[2rem]">
            Dynamically created gallery of user uploaded photos
        </section>
    )
}